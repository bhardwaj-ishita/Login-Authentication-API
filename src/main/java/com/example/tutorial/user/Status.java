package com.example.tutorial.user;

/**
 * Enum is not being used. It is used in the original API format.
 */
public enum Status {
    //give feedback to the user as to whether their current action was successful or whether it failed
    SUCCESS,
    USER_ALREADY_EXISTS,
    FAILURE
}
